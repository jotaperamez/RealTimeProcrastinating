with Ada.Text_IO;
use Ada.Text_IO;

procedure ejercicio01_05 is
   -- Crea la constante
   cadena: constant String := "TIEMPO REAL";
begin
   --Imprime por pantalla la constante
   Put_Line(cadena);
end ejercicio01_05;
